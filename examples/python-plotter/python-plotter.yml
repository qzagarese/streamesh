name: python-plotter
image: "python-plotter:latest"
inputMapping:
  baseCmd: python3 /usr/plot_1.py
  parameters:
    - externalName: dataset-url
      internalName: --url
      optional: false
outputMapping:
  outputDir: /tmp
  outputFileName: count_of_room_type_by_neighbourhood.png


